# Homebrew Tap

Homebrew casks published by [Disconnecter](https://github.com/Disconnecter).

## MicStatusAI

After the first MicStatusAI release is published:

```sh
brew tap Disconnecter/tap
brew install --cask micstatusai
```

Cask updates are generated by the MicStatusAI release workflow.
